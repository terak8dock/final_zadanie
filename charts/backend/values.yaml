replicaCount: 1
image:
  repository: terak8dock/appbackend
  pullPolicy: IfNotPresent
  tag: "1.0.0"
service:
  type: ClusterIP
  port: 9080

mongodb:
  auth:
    enabled: true
    rootPassword: rootpass
    username: user
    password: password
    database: mydatabase